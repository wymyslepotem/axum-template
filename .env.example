HTTP_HOST=127.0.0.1
HTTP_PORT=3000
APP_ENV=development
LOG_FORMAT=pretty

# "" disables CORS, "*" allows any origin, or provide a comma-separated allowlist.
APP_CORS_ORIGINS=""

# Provide to enable rate limiting.
APP_RATELIMIT_RPS=50
APP_RATELIMIT_BURST=100
APP_RATELIMIT_TRUST_PROXY=false
